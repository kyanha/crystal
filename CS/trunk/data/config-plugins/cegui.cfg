; Path to the CEGUI log file. This path is a real, physical, path, unless
; it is preceded by "VFS:" which then means that this is a path within
; the Virtual File System.
CEGUI.LogFile = VFS:/tmp/CEGUI.log
